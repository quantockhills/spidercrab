# Documentation site dependencies (MkDocs Material)
# Install:  pip install -r docs/requirements.txt
# Preview:  python -m mkdocs serve
# Deploy:   python -m mkdocs gh-deploy   (pushes to the gh-pages branch)
mkdocs-material>=9.7,<10
